# LDA 六特性波次 · 实施与验证报告

**日期**：2026-08-31
**场景**：全流程交付（产品评审 → 并行实现 → 串行接线 → 安全审计 + QA → 修复 → 验证）
**参与成员**：产品官（gstack-product-reviewer）+ 安全卫士（gstack-security-officer）+ 质量门神（gstack-qa-lead）
**基线 → 结果**：`f6abe2c`（1404 测试）→ **`a1fa582`（1655 测试，0 失败，串行验证）**
**规模**：96 文件，+16362 / -596

---

## 📌 TL;DR

- 整体结论：🟢 **通过**（Critical 1 + Blocker 1 + High 5 全部修复并验证）
- 六个特性全部交付；7/7 审计与 QA 发现已闭环
- 最有价值的产出不是特性本身，而是**审计发现的一类共性缺陷**：同一条不变量存在两份实现，或未贯穿组合边界
- 下一步：安装到本机；三项已知缺口留作后续（见"待完善"）

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 Go |
| 严重度分布（修复前）| 🔴 Critical 1 · 🔴 Blocker 1 · 🟠 High 5 · 🟡 Medium 15 · 🟢 Low 14 |
| 严重度分布（修复后）| 🔴 0 · 🟠 0（P2 项转为后续任务） |
| 测试增长 | 1404 → 1655（+251） |
| 关键行动项 | 3 条（见下） |

---

## 1. 各成员核心结论

### 🔍 产品官（波次前的 Autoplan 评审）
- 核心判断：**SELECTIVE EXPANSION**。推翻了草案的三个前提——F1b 的"既有手动框选 UI"根本不存在、F3 比设想小得多（tray 与队列已在）、F2 草案的容器结构有安全硬伤。
- 关键建议（全部被采纳为不可协商裁决）：F2 必须 **package-then-encrypt**（外层 zip 的中央目录以明文存条目名，而条目名就是当事人姓名）；F5 覆盖文本**仅限 pseudonym 风格**（token 风格下重蹈静默零恢复）；F4 作用域键**只带 UUID**；F1 印章跨度必须有机构名 payload。
- 修正了波次划分：草案的"W1 四特性互不相交"不成立，三个特性都要动 SessionModel，遂改为 W1 引擎并行 / W2 单人串行接线 / W3 工作区收尾。

### 🛡️ 安全卫士（OWASP + STRIDE）
- 核心判断：🟡 条件 Go，但**字面还原路径在 C1/H1 修复前应视为 🔴**。
- 最重要的发现（C1）：**DOCX 写入器与还原报告是两份实现，跑在两个不同的字符串上**。run 切分使写入器看不见掩码冲突 → 文书里写入**错误当事人的真实姓名**，而报告声称"未做任何猜测"。对一款以可核验为卖点的法律工具，这比泄漏更难解释。
- 实测验证**成立**的不变量：密文中无文件名/实体明文/matter 标签/zip 签名；打开零 Keychain 依赖；按实际膨胀字节计量；zip-slip 与 symlink 成员结构性阻断；PBKDF2 600k + 头部作 AAD；`URLSession` 仅存在于 ModelInstaller。

### ✅ 质量门神（真实端到端 QA）
- 核心判断：🔴 **No-Go**，单一阻断项。
- Blocker：**截断的 SEAL 跨度顶掉更宽的 COMPANY 跨度**，`ABC科技有限公司公章` → `ABC{SEAL_1}`，机构名头部明文流入待发给 AI 的文件。**这是相对波次前的退化**，且 CLI/MCP 路径无人工复核。
- 实测通过：token/pseudonym 双风格 CLI 闭环 byte-identical；`.ldawork` 跨机交接（含 SEAL + 自定义假名 + matter 名单）全项一致且未泄漏进全局层；图像红章覆盖后残留红墨像素 0；纯样板文本零 SEAL 检出；两个"故意断言错误行为"的 known-gap 测试仍在、仍通过、注释诚实。

---

## 2. 综合审查发现（已全部闭环）

| # | 严重度 | 类别 | 问题 | 修复 |
|---|--------|------|------|------|
| 1 | 🔴 Blocker | PII 泄漏（退化） | 截断 SEAL 跨度顶掉 COMPANY，机构名头部明文残留 | `SpanMerger` 改为**吸收**：胜出者保留身份，range 扩张到冲突并集；以 400 组随机跨度的属性测试钉住"接受集恰好覆盖候选并集" |
| 2 | 🔴 Critical | 完整性 | DOCX 写入器与报告判定不一致，写错人名却报告"未猜测" | join-decide-writeback：`literalRestoreSites` 成为**唯一**判定函数，报告扫描 / 纯字符串 / docx walker 三条路共用 |
| 3 | 🟠 High | 完整性 | 自定义假名绕过 seam guard（`代理人` + 原文 `甲方` 拼出 `甲方代理人`）| 覆盖文本纳入 `PseudonymSeamGuard`，前后向接缝同查，新增 `seamSpellsAnotherReplacement` |
| 4 | 🟠 High | DoS | 嵌套归档预算不复合（工作区 + 文件夹两个新入口，后者无需口令）| `ArchiveBudget` 账本贯穿单次导入；工作区加可开类型白名单；文件夹导入移除 zip 展开 |
| 5 | 🟠 High | 越权/隐私 | 导出在途切换 matter，学习规则落进全局层 | 在 await **之前**快照 store 与写入目标；用阻塞式 completer 复现竞态，红→绿验证 |
| 6 | 🟠 High | 泄漏 | 图像印章候选**从未接入 GUI**（CLI 遮盖，App 不遮）| 接线 + 计数与未定位区间警告上屏 + 每文档开关 |
| 7 | 🟠 High | 泄漏 | 合规报告明文写入当事人标签与全部文件名 | 默认导出为加密 `.ldareport`（口令规则**委托**给工作区规则，杜绝漂移）；明文另存降级为显式选项并明示后果 |

---

## ✅ 行动清单

| # | 行动 | 紧急度 | 状态 |
|---|------|--------|------|
| 1 | 安装到本机并做一次真实文档冒烟 | P0 | 进行中 |
| 2 | 测试套件并发不隔离（同一 commit 12 失败 / 0 失败）：按进程唯一化 Keychain 账户与 UserDefaults 域 | P1 | 已建任务 |
| 3 | 跨文档 seam 缺口与 shape 4：需要对整体分配做 repair pass，非 mint 时过滤 | P1 | 已建任务 |

---

## ⚠️ 待完善 / 已知局限

- **假名铸造性能**：O(实体数 × 文档长度)，570 KB / 15000 实体未能在 7 分 50 秒内完成。seam guard 的正确性代价，需增量化 `renderProvisional`。
- **两个 known-gap**：shape 4（前文接缝吞掉后续站点）与跨文档接缝，均以"故意断言错误输出"的测试钉住，**不得**通过改期望值"修复"。
- **asterisk 掩码碰撞时序**：同公司多枚印章必然碰撞，但用户只在还原时才知道——应在脱敏阶段就预警。
- **拖放文件夹**不走 F3 展开（仅打开面板走），两个入口行为不一致。
- **DOCX 非正文部件**（页眉/页脚/脚注）会被还原写入但从不进报告——先于本波次存在，token 风格同样如此。
- **XPC 拆分**仍是 design-only，本波次任何特性都不以它为前提。

---

## 📚 过程备忘（对后续波次有价值）

1. **`git stash` 是仓库级而非 worktree 级**：两个 agent 同时 stash 会互换文件，`pop` 报成功但还原错内容。已两次独立触发。
2. **测试套件并发不隔离**：`--scratch-path` 只隔离构建产物，测试仍共用系统 Keychain 与 UserDefaults。**只有串行运行的数字可用于发布决策**。
3. **不要把根因分析当事实转述**：QA 报告称 SEAL 是 COMPANY 的严格子集，实测是**部分重叠**（`[6,14)` vs `[3,12)`），据此提出的两个修复方案**都不成立**。执行方实测几何后自行推翻，是本波次最有价值的一次拒绝服从。
4. **断言形状决定能否抓到缺陷**：`testSealBeatsLLMCompanyClaimOnMerge` 只测超集情形，且邻近注释断言"LLM COMPANY 仍拥有拉丁名"——该断言在合并层为假且无测试支撑，Blocker 正藏在那里。新测试一律断言**脱敏输出文本**不含红线子串，而非断言跨度类型。

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
